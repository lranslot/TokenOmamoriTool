<Window x:Class="TokenOmamoriTool.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{DynamicResource Settings_Title}" SizeToContent="WidthAndHeight"
        Icon="/Assets/omamori.ico" Background="{StaticResource WashiBackground}"
        MinWidth="300" MaxWidth="420" ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">
    <StackPanel Margin="12">
        <TextBlock Text="{DynamicResource Settings_MaxLines}" FontSize="11" />
        <TextBox x:Name="MaxLinesBox" Margin="0,2,0,8" />

        <TextBlock Text="{DynamicResource Settings_MaxSizeKB}" FontSize="11" />
        <TextBox x:Name="MaxSizeKBBox" Margin="0,2,0,8" />

        <TextBlock Text="{DynamicResource Settings_SessionLogMaxMB}" FontSize="11" />
        <TextBox x:Name="SessionLogMaxMBBox" Margin="0,2,0,8" />

        <TextBlock Text="{DynamicResource Settings_PollInterval}" FontSize="11" />
        <TextBox x:Name="PollIntervalBox" Margin="0,2,0,8" />

        <TextBlock Text="{DynamicResource Settings_ExternalToolsInterval}" FontSize="11" />
        <TextBox x:Name="ExternalToolsIntervalBox" Margin="0,2,0,8" />

        <CheckBox x:Name="HideOnCloseBox" Content="{DynamicResource Settings_HideOnClose}" FontSize="11" Margin="0,2,0,4" />
        <CheckBox x:Name="ToastOnWarningBox" Content="{DynamicResource Settings_ToastOnWarning}" FontSize="11" Margin="0,0,0,8" />

        <TextBlock x:Name="ErrorText" Foreground="Red" FontSize="10" TextWrapping="Wrap"
                   Visibility="Collapsed" Margin="0,0,0,4" />

        <Grid Margin="0,6,0,0">
            <Button Content="{DynamicResource Settings_Reset}" Click="ResetButton_Click" Padding="10,3" HorizontalAlignment="Left" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource Common_Save}" Click="SaveButton_Click" Padding="10,3" Margin="0,0,6,0" />
                <Button Content="{DynamicResource Common_Cancel}" Click="CancelButton_Click" Padding="10,3" />
            </StackPanel>
        </Grid>
    </StackPanel>
</Window>
