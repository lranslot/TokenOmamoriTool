<Window x:Class="TokenOmamoriTool.ToolOperationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{DynamicResource ToolOp_Title}" SizeToContent="WidthAndHeight"
        Icon="/Assets/omamori.ico" Background="{StaticResource WashiBackground}"
        MinWidth="420" MaxWidth="560" MaxHeight="640" ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
    <DockPanel Margin="12">
        <TextBlock x:Name="TitleText" DockPanel.Dock="Top" FontWeight="Bold" FontSize="13" Margin="0,0,0,8" TextWrapping="Wrap" />

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Right">
            <Button x:Name="RunButton" Content="{DynamicResource ToolOp_Run}" Click="RunButton_Click" Padding="10,3" Margin="0,0,6,0" />
            <Button x:Name="CloseButton" Content="{DynamicResource Common_Close}" Click="CloseButton_Click" Padding="10,3" />
        </StackPanel>

        <!-- HorizontalScrollBarVisibility=Disabled forces a finite width on the items so long
             descriptions wrap instead of overflowing sideways. -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="StepsList">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel>
                            <!-- Grid (not horizontal StackPanel) so the description gets a finite
                                 width and can actually wrap in both languages. -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding StatusIcon}" FontWeight="Bold" />
                                <TextBlock Grid.Column="1" Text="{Binding Description}" TextWrapping="Wrap" />
                            </Grid>
                            <TextBlock Text="{Binding CommandPreview}" FontFamily="Consolas" FontSize="10"
                                       Foreground="Gray" TextWrapping="Wrap" Margin="40,2,0,0" />
                            <TextBlock Text="{Binding ResultMessage}" FontSize="10" TextWrapping="Wrap"
                                       Margin="40,2,0,0" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
